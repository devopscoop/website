# Arch Linux package list for website — the pacman/AUR equivalent of the
# Brewfile. Installs every CLI tool used or referenced by this repo.
#
# All packages are in the official repos, so plain pacman works (as do yay/paru):
#
# Usage:
#   grep -vE '^(#|$)' pkglist.txt | sudo pacman -S --needed -

# dart-sass (`sass`) — compiles public/sass/*.scss to public/assets/css/.
# public/Dockerfile pins DART_SASS_VERSION=1.70.0; pacman installs latest.
dart-sass

# docker — running the documented build via public/Dockerfile
docker

# git — cloning and contributing
git
